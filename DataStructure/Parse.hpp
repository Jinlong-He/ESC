#include "Model.hpp"
#include <string>

class Xml {

};

class Parse {
private:
    Model* model;
    Xml* xml;
public:
    void get_model() {
        //get @pram model form @param xml.
        //call get_process, get_ik, get_property.
    }

    Process* get_process(Xml* x) {
        //call get_fst.
    }

    Process* get_fst(Xml* x) {
    }

    Process* get_property(Xml* x) {
    }

    Process* get_ik(Xml* x) {
    }

};
